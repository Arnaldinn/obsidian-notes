
Após capturar hashes ou senhas, o próximo passo é **validar** se essas credenciais funcionam em algum serviço do ambiente Windows.  
Essa etapa confirma se o usuário existe, se a senha está correta e se ele possui algum nível de acesso útil.

Os serviços SMB (Server Message Block) são os melhores alvos para essa validação, pois a maioria das máquinas Windows aceita autenticação via SMB.

---

# 🧪 Testando credenciais com smbclient

O `smbclient` é um cliente SMB do Linux usado para acessar compartilhamentos de arquivos em computadores Windows.

Com ele podemos:

- validar usuário e senha  
- verificar se o login é aceito  
- listar compartilhamentos  
- identificar máquinas acessíveis  

### ✔ Comando principal:

```
smbclient -L //<IP_DO_SERVIDOR> -U <USUARIO>
```


O **`psexec.py`** é um script da **Impacket** que permite **execução remota de comandos** em sistemas Windows via **SMB (Server Message Block)**. Ele é amplamente usado em **pentests**, especialmente para ganhar **acesso remoto a máquinas Windows** após comprometer credenciais.

```
psexec.py ORIONSCORP2/rlourdes:senha123@192.168.1.10
```

O psexec.py esta nesse diretorio:

```
python3 /usr/share/doc/python3-impacket/examples/psexec.py orionscorp2/rlourdes:'georgeorwell1984'@172.16.1.253
```

**PARTE IV - Enumeration AD**



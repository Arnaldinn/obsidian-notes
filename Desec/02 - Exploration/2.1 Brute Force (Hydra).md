
O **Hydra** é uma ferramenta de força bruta usada para testar autenticação em diversos serviços,
como **SSH, RDP, SMB, FTP, HTTP, MySQL**, entre outros.

Ele tenta combinações de usuários e senhas até encontrar credenciais válidas, permitindo identificar logins fracos ou reutilizados.

---

# 🧠 Como o Hydra funciona (explicação simples)

- Ele abre várias **conexões simultâneas** para o serviço alvo.  
- Testa combinações de **usuário + senha** retiradas de listas (`wordlists`).  
- Quando uma combinação funciona, ele retorna:  
  **[SUCCESS] login:senha**  
- Pode ser usado tanto para auditoria quanto para pentest interno.

---

# 🧩 Comando básico do Hydra

```
hydra -v -L usuários.txt -P senhas.txt <SERVIÇO>://<IP>:<PORTA>
```


`-L usuários.txt` → Arquivo com a lista de usuários.
`-P senhas.txt` → Arquivo com a lista de senhas.
`<SERVIÇO>` → O protocolo (ex: ssh, ftp, smb, http, mysql).
`<IP>` → O alvo.
`<PORTA>` → A porta do serviço.

Exemplos de uso:

```
hydra -l root -P /usr/share/wordlists/rockyou.txt ssh://192.168.1.100 -t 4
```

`-t 4` → Usa 4 threads (aumenta a velocidade do ataque).